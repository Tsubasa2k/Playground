#include<iostream>
using namespace std;
int main()
{
  //Type your code here.
  int r, s;
  cin>>r>>s;
  if (2 * r <= s)
    cout<<"circle can be inside a square";
  else
    cout<<"circle cannot be inside a square";
}